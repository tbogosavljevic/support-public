version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2204
blocks:
  - name: Initial Block
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - 'pr_data="skip ci"; [[ $pr_data == *"skip ci"* ]] && echo "contains"'
